# ==============================================================================
#                 FICHIER DE CONFIGURATION PRINCIPAL - KASPERBOT v8.0
# ==============================================================================

# -- Identifiants de connexion à MetaTrader 5 --
mt5_credentials:
  login: 10004952
  password: "jsne80aX!"
  server: "Axi-US50-Demo"

# -- Paramètres de l'interface web (API) --
api:
  host: "127.0.0.1"
  port: 5000

# -- Paramètres de trading --
trading_settings:
  symbols: 
    - "XAUUSD"
    - "EURUSD"
    - "GBPUSD"
    - "BTCUSD"
    - "ETHUSD"
  timeframe: "M15"
  live_trading_enabled: True
  magic_number: 13579

# -- Gestion du risque --
risk_management:
  risk_per_trade: 0.01
  daily_loss_limit_percent: 0.05
  sl_tp_strategy: "ATR_MULTIPLE"
  
  # NOUVEAU: Réglages ATR avec un défaut et des surcharges par symbole
  atr_settings:
    default:
      period: 14
      sl_multiple: 1.5
      tp_multiple: 3.0
    XAUUSD: # Paramètres spécifiques pour l'Or
      sl_multiple: 1.8
      tp_multiple: 3.5
    BTCUSD: # Paramètres spécifiques pour le Bitcoin
      sl_multiple: 2.5
      tp_multiple: 4.0

  breakeven:
    enabled: true
    trigger_pips: 100
    pips_plus: 10

  trailing_stop:
    enabled: true
    trailing_pips: 150
    activation_pips: 200

# -- Détection de Patterns Smart Money Concepts (SMC) --
pattern_detection:
  SMC_AMD_SESSION: true
  INBALANCE: true
  ORDER_BLOCK: true

# -- Filtre de Tendance (Order Flow) --
trend_filter:
  enabled: true
  ema_period: 200