# ==============================================================================
#                 FICHIER DE CONFIGURATION PRINCIPAL - KASPERBOT v6.0
# ==============================================================================

# -- Identifiants de connexion à MetaTrader 5 --
mt5_credentials:
  login: 10004952
  password: "jsne80aX!"
  server: "Axi-US50-Demo"

# -- Paramètres de l'interface web (API) --
api:
  host: "127.0.0.1"
  port: 5000

# -- Paramètres de trading --
trading_settings:
  symbol: "XAUUSD"
  timeframe: "M15"  # Recommandé pour le SMC Day Trading
  live_trading_enabled: false
  magic_number: 13579

# -- Gestion du risque --
risk_management:
  risk_per_trade: 0.01
  daily_loss_limit_percent: 0.05
  
  sl_tp_strategy: "ATR_MULTIPLE"
  
  atr_settings:
    period: 14
    sl_multiple: 1.5  # SL plus serré pour les entrées précises SMC
    tp_multiple: 3.0  # Viser un R:R de 1:2 ou 1:3 est commun en SMC

  breakeven:
    enabled: true
    trigger_pips: 100
    pips_plus: 10

  trailing_stop:
    enabled: true
    trailing_pips: 150
    activation_pips: 200

# -- Détection de Patterns Smart Money Concepts (SMC) --
# Le bot va maintenant rechercher des setups SMC complets (ex: AMD).
pattern_detection:
  SMC_AMD_SESSION: true