# Fichier: config.yaml
# Version: 14.0.0 (Guardian+ Enhanced)

api:
  host: 127.0.0.1
  port: 5000

# IMPORTANT: Remplir avec vos propres identifiants
mt5_credentials:
  #login: 60035780
  #password: "Quench-Quantum4"
  #server: "Axi-US51-Live"
  login: 10004952
  password: "jsne80aX!"
  server: "Axi-US50-Demo"
trading_settings:
  # false = mode simulation (aucun ordre réel), true = mode réel
  live_trading_enabled: false
  magic_number: 13579
  symbols:
  - XAUUSD
  - EURUSD
  - GBPUSD
  - NZDUSD
  - AUDUSD
  - USDCAD
  - USOIL
  
  timeframe: M15

risk_management:
  # Pourcentage du capital à risquer par trade (ex: 0.01 = 1%)
  risk_per_trade: 0.01
  # Disjoncteur: arrête le trading pour la journée si ce % de perte est atteint
  daily_loss_limit_percent: 2.0
  sl_tp_strategy: ATR_MULTIPLE
  atr_settings:
    default:
      period: 14
      sl_multiple: 1.5
      tp_multiple: 3.0
    XAUUSD:
      sl_multiple: 1.8
      tp_multiple: 3.5
  breakeven:
    enabled: true
    trigger_pips: 100 # Pips de profit requis pour activer le BE
    pips_plus: 10    # Déplace le SL de X pips en profit
  trailing_stop_atr:
    enabled: true
    activation_multiple: 2.0 # Le TS s'active quand le prix a bougé de X * ATR en profit
    trailing_multiple: 1.8   # Le SL suit le prix à une distance de X * ATR

pattern_detection:
  ORDER_BLOCK: true
  CHANGE_OF_CHARACTER: true
  INBALANCE: true
  LIQUIDITY_GRAB: true
  SMC_AMD_SESSION: true

trend_filter:
  enabled: true
  higher_timeframe: H4
  ema_period: 200

learning:
  enabled: false
  min_trades_for_analysis: 10